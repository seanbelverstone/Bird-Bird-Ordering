import axios from "axios";

export default {

	// order: function () {
	// 	return await axios.post('http://localhost:7000/api/stripe/charge', {
	// 	amount: selectedProduct.price.toString().replace('.', ''),
	// 	source: token.id,
	// 	receipt_email: 'customer@example.com'
	// 	}
	// }
}